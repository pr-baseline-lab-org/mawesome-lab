---
'@mawesome/pr-baseline': minor
---

New package: keep open pull requests current with a movable baseline ref on the base branch, reported through commit statuses via a CLI, a programmatic API and a GitHub Action.

---
'@mawesome/pr-baseline': minor
---

New package: keep open pull requests current with a movable baseline tag on the base branch, reported through commit statuses via a CLI and a programmatic API.
